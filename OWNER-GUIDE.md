# 📘 دليل مالك التطبيق — ElForma

> الملف ده هو البوابة. لو عايز تفهم أو تعدّل أو تنقل أو تصيّن التطبيق — ابدأ من هنا.
> كل الملفات مكتوبة بالعربي وبقيم مشروعك الحقيقية، مش كلام عام.

## التطبيق ده إيه في سطر واحد؟

تطبيق لياقة وتغذية: **تطبيق موبايل Flutter** (أندرويد دلوقتي، ونفس الكود يصلح iOS) بيكلم
**سيرفر Node.js** واحد، والسيرفر بيكتب ويقرا في **قاعدة بيانات SQLite** (محلية على الجهاز
أو سحابية عبر Turso). فيه كمان موقع ويب ولوحة أدمن بيتقدموا من نفس السيرفر.

## 🗺️ الخريطة السريعة — كل حاجة مكانها فين

| الحتة | مكانها | وظيفتها |
|---|---|---|
| **التطبيق (الموبايل)** | `mobile/` | شاشات Flutter — `lib/screens/` الشاشات، `lib/api.dart` التخاطب مع السيرفر |
| **السيرفر** | `server.js` + `api/` + `lib/` | استقبال الطلبات، الحسابات، الحماية، الكاش |
| **عقل التغذية** | `app/diet/js/` | محرك الوجبات (نفس الملفات بتشتغل في المتصفح وعلى السيرفر) |
| **عقل التمرين** | `app/workout/engine/` | محرك الجداول التدريبية |
| **الموقع + لوحة الأدمن** | `public/` | صفحات ثابتة |
| **الاختبارات** | `test/` | 61 حارس آلي — شغّلهم بـ `npm run test:all` |
| **إعدادات النشر** | `render.yaml` + `Dockerfile` | إزاي السيرفر بيقلع |
| **بناء الـ APK** | `.github/workflows/flutter-apk.yml` | GitHub بتبني التطبيق لوحدها مع كل رفع |

## 📚 الفهرس — لو عايز تعمل كذا اقرأ كذا

| لو عايز... | اقرأ |
|---|---|
| تفهم المعمارية وماشية إزاي | `docs/01-ARCHITECTURE.md` |
| تفهم المتغيرات (الأسرار والمفاتيح) | `docs/02-CONFIG.md` |
| تنقل من سيرفر لسيرفر (Render → VPS مثلًا) | `docs/03-MOVE-SERVER.md` |
| تنقل من قاعدة لقاعدة (Turso → محلي → Postgres) | `docs/04-MOVE-DATABASE.md` |
| تصيّن يومي: نشر، مراقبة، رجوع، أخطاء شائعة | `docs/05-MAINTENANCE.md` |

## ⚠️ أهم 5 تحذيرات (احفظهم)

1. **`EF_SECRET` مايتغيرش أبدًا بعد أول مستخدم** — تغييره = خروج كل المستخدمين فورًا.
2. **متغيرات اللوحة بتغلّب `render.yaml`** — لو غيّرت حاجة في لوحة Render، اللوحة هي اللي بتكسب. عشان كده أي تغيير دائم اعمله في `render.yaml`.
3. **عنوان السيرفر مبني جوه الـ APK** — لو غيّرت الدومين لازم بناء تطبيق جديد (التفاصيل في `docs/03-MOVE-SERVER.md`).
4. **`EF_TURSO_MODE=replica` معطّلة عن قصد** — قاعدتك بترفضها (WalConflict). الكود بيحمي نفسه ويرجع `remote` لوحدها، بس متشغلهاش. (القصة في `docs/04-MOVE-DATABASE.md`)
5. **مترفعش كود من غير ما الاختبارات تعدي** — الـ CI بيعملها لوحده، بس لو شغّل يدوي: `npm run test:all`.

## 📊 أرقام مرجعية (مقاسة فعليًا — سبتمبر 2026)

- فتحة التطبيق (bootstrap): **~3ms محليًا**، وتتحمل **+3,000 طلب/ثانية** على جهاز ضعيف
- 200 كتابة متزامنة: كلها ناجحة، p99 = 48ms
- القراءات مابتتأثرش بحساب الخطط التقيلة (الـ worker pool شغال)
- إعادة القياس في أي وقت: `node tools/load-test.js`

## 🆘 طوارئ سريعة

| الموقف | اعمل إيه |
|---|---|
| الموقع وقع | لوحة Render → Logs → لو السبب مش واضح، رجّع آخر نسخة شغالة (Deploys → اختار القديم → Redeploy) |
| التطبيق بطيء فجأة | افتح `/api/admin/ops` بحساب الأدمن وشوف `memoryMb` و `enginePool.queued` |
| عايز أعرف السيرفر شغال | `https://elforma.onrender.com/api/health` لازم ترد 200 |
