<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The Flutter tool needs INTERNET to talk to the running app for hot
         reload and breakpoints. Debug/profile only -- never in release. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
