# تقرير المراجعة الشاملة — تطبيق الفورمة (ElForma)

**التاريخ:** 10 سبتمبر 2026 · **النطاق:** فحص وإصلاح 3 مشاكل محددة + مراجعة العقلين + المتابعة الذكية + الأداء وقابلية التوسع
**المنهجية:** قراءة كاملة للكود (Flutter + Node.js + Turso + Upstash + Render) → تشخيص جذري → إصلاح جراحي بدون إعادة بناء → تحقق آلي (54 حزمة اختبار + اختباران جديدان مكتوبان خصيصًا)

---

## 1) المشكلة الأولى — بطء شاشة البداية

### السبب الحقيقي (مدعوم بالكود)
السبب مركّب، والاستضافة نصيبها الأساسي لكن العميل كان بيزوّد المدة بشكل ملموس:

1. **سكون Render المجاني (السبب الأساسي):** أول طلب بعد فترة خمول ينتظر إيقاظ الخدمة (~40–60 ثانية).
2. **العميل كان يزيدها (السبب اللي عندنا):**
   - أول طلب شبكة كان بيتأخر لحد ما تخلص قراءة التخزين المشفّر المحلي — يعني إيقاظ السيرفر ماكانش بيبدأ من أول ميلي ثانية.
   - مهلة القراءة كانت **30 ثانية** للطلب الواحد: طلب معلّق على سيرفر نايم لا يُسرِع الإيقاظ، لكنه يمنع إعادة المحاولة السريعة.
   - جدول إعادة المحاولة كان محدودًا: محاولتان فقط بفواصل 5 ثم 15 ثانية → أسوأ حالة ~110 ثانية قبل ظهور رسالة خطأ، والمستخدم طول الوقت شايف اللوجو من غير أي تفسير.

### ما تم إصلاحه (mobile)
- **`warmup()`:** نداء إيقاظ فوري لـ `/api/health` ينطلق في أول ميلي ثانية من شاشة البداية، بالتوازي مع قراءة التخزين المحلي — السيرفر يبدأ يصحى فورًا.
- **مهلة إقلاع أقصر (12 ثانية)** لنداءَي البوابة والجلسة: إعادة الطرق أسرع وأنجح من الانتظار المعلق.
- **جدول محاولات يغطي نافذة الإيقاظ كاملة:** 5 محاولات بفواصل [3, 4, 6, 8, 10] ثوانٍ ≈ تغطية حتى ~103 ثانية بدل الاستسلام المبكر أو الانتظار الأعمى.
- **رسائل مرحلية صادقة تحت المؤشر:** بعد 6 ثوانٍ «بنجهز الاتصال بالخادم…» وبعد 20 ثانية «الخادم بيصحى من وضع السكون — أول فتحة ممكن تاخد حتى دقيقة» — الانتظار بقى مفهومًا بدل ما يبدو كأن التطبيق واقف.

### توصيات استضافة (قرارك أنت)
- **الحل الجذري الوحيد للسكون:** خطة Render مدفوعة (~$7/شهر) تلغي السكون نهائيًا — أنصح بها لأي منتج مدفوع.
- **بديل مجاني فعّال:** خدمة مراقبة خارجية (UptimeRobot / cron-job.org) تطرق `/api/health` كل 5–10 دقائق في ساعات الذروة — تبقي الخدمة صاحية فالمستخدم لا يدفع ثمن الإقلاع البارد أصلًا. (لم أضف self-ping داخلي في الكود احترامًا لسياسة الخطة المجانية.)

---

## 2) المشكلة الثانية — صفحة التمرين (رسالة «تعذر التحميل» + شريط التحميل الدائم)

### السبب الحقيقي
الجدول نفسه كان بيتحمّل ويظهر (الصورة اللي بعتها بتثبت ذلك — الخطة ظاهرة تحت البانر). اللي بيحصل:

- دالة `_generate()` كانت بتتنادى من **7+ أحداث خلفية**: فتح الصفحة، مستمعو PlanStore/ProfileStore/SubscriptionStore، مؤقّت منتصف الليل، الرجوع للتطبيق من الخلفية، والسحب للتحديث.
- كل نداء كان بيرفع `LinearProgressIndicator` فوق الصفحة → **الشريط اللي بيتحرك طول الوقت** اللي لاحظته.
- وأي هفوة شبكة مؤقتة (طبيعية مع استضافة مجانية + قاعدة Turso البعيدة) أثناء تحديث خلفي كانت بتسجّل `_error` → **البانر الأحمر «تعذر التحميل» فوق جدول معروض وشغال** — رسالة مخيفة على لا شيء.

### ما تم إصلاحه (mobile)
- **وضع التحديث الصامت (silent refresh):** كل المحفّزات الخلفية (مستمعو المخازن، منتصف الليل، الرجوع من الخلفية، فتح الصفحة مع جدول محفوظ) بتشتغل الآن بدون شريط تحميل وبدون بانر خطأ طالما فيه جدول معروض.
- **المؤشرات ورسائل الخطأ بقت مربوطة بالفعل اليدوي:** السحب للتحديث، زر إعادة المحاولة، توليد بدائل، واسترجاع الجدول — هي فقط اللي تُظهر التحميل والأخطاء. أول تحميل حقيقي (لا جدول بعد) يظل يعرض السبينر الكامل كما هو متوقع.
- لم يتغير أي منطق توليد أو حفظ — تغيير عرض فقط، بالضبط كما طلبت: «بتحمل في نقط معينة أو ريفريش».

---

## 3) المشكلة الثالثة — التحميل العالق في نهاية الشات

### السبب الحقيقي (مُقاس بالأرقام)
- زر النهاية في الشات كان ينادي `PUT /api/mobile/profile` بـ `preparePlans: true`.
- السيرفر كان ينفّذ `prepareSavedPlans` **متزامنًا داخل نفس الطلب**: توليد جدول التمرين كاملًا (على الخيط الرئيسي) + توليد خطة التغذية كاملة (عبر الـ worker pool) + فحص الفيديوهات + محاذاة الجدول + كتابات متعددة إلى Turso البعيد — كل ذلك والمستخدم واقف في الشات أمام عنصر تحميل باهت غير قابل للضغط، بمهلة عميل تصل إلى 70 ثانية.

### ما تم إصلاحه (server + mobile)
- **مسار تجهيز مخصص جديد:** `POST /api/mobile/plans/prepare` — نفس منطق `prepareSavedPlans` بالكامل (نفس أقفال الأمان والتحققات)، مع:
  - **dedupe لكل مستخدم:** طلبان متزامنان (صفحة التحليل + إعادة محاولة) يتشاركان نفس التشغيلة بدل تشغيل المحرك مرتين.
  - **idempotent:** إعادة التجهيز لا تستبدل الخطة ولا تمسح نقطة بداية الرحلة (مثبت باختبار).
- **الشات الآن يحفظ البروفايل فقط** — مُقاس في الاختبار: **144ms** بدل عشرات الثواني — وينتقل فورًا لصفحة التحليل الموجودة أصلًا.
- **صفحة التحليل** (الصفحة اللي في النص اللي قصدك عليها) هي اللي تبدأ تجهيز الخطط بواجهة الانتظار المصممة (مراحل «بنراجع بيانات جسمك…»)، بالتوازي مع جلب الأهداف — فالداشبورد يتكشف أول ما السعرات تجهز، والتابات تلاقي الخطط جاهزة. وفشل التجهيز المسبق غير قاتل: تابا التمرين والتغذية يحسبان عند الطلب تلقائيًا (مسار موجود مسبقًا).
- **حفاظ على العقود:** عقد `preparePlans` المتزامن في `PUT /api/mobile/profile` لم يتغير — اختبار `root-review-125` يظل أخضر.

---

## 4) إصلاح إضافي اتكشف أثناء الفحص (بق موجود أصلًا)

**مفتاح اليوم في كتابات اليومية:** حفظ التغذية/الوزن/المقاسات كان يستخدم تاريخ **UTC الخاص بالسيرفر** عند غياب `day` من الطلب (وتطبيق الموبايل لا يرسله)، بينما القراءة في bootstrap تستخدم يوم الجهاز (هيدر `X-Tz-Offset` بافتراضي +180). النتيجة: أي حفظ بين 12 و3 الفجر بتوقيت القاهرة كان ينزل على اليوم السابق ولا يظهر في «النهارده». **الإصلاح:** دالة `requestDay()` توحّد القاعدة بين الكتابة والقراءة. الأثر الجانبي الحسن: اختبار `mobile-nutrition` الذي كان **راسبًا قبل تعديلاتي** أصبح يمر الآن.

---

## 5) مراجعة العقلين — بصفتي مدربًا وأخصائي تغذية

راجعت الكود الفعلي للعقلين سطرًا بسطر (ليس مجرد أسماء ملفات). الحكم: **العقلان حقيقيان وعميقان — وليسا ديكورًا ولا قواعد ثابتة.**

### عقل التمرين — `lib/coach-progression.js` + المحرك
- **Double Progression حقيقي:** زيادة حمل 5كجم للمركبات السفلية، 2.5 للمركبات، 1.25 للعزل؛ زيادة تكرارات حتى أعلى المدى ثم زيادة وزن.
- **معالم حجم أسبوعية MEV/MAV/MRV لكل عضلة حسب الخبرة** (Renaissance Periodization) مع حالة حجم مكتوبة بالعربية (تحت MEV / منطقة البناء / الأمثل / فوق MRV).
- **RIR waving عبر الميزوسايكل** + أسبوع Deload تلقائي (حمل 90%، مجموعات 60%).
- **إدارة الثبات:** فشلان متتاليان ← تخفيض 10% وإعادة بناء (back-off) — مش رد فعل عشوائي.
- **مقاييس علمية:** e1RM (Epley)، الشدة النسبية، التكرارات المحفزة، الحمولة (tonnage)، و**ACWR** مع مناطق تحذير.
- **ذاكرة حقيقية:** `replayLift()` يعيد بناء حالة كل تمرين من سجل الجلسات الفعلي في قاعدة البيانات — يعني القرارات مبنية على تاريخ المتدرب الحقيقي عبر الأجهزة، مش على تخزين متصفح عابر.

### عقل التغذية — `lib/diet-periodization.js` + `lib/plateau-diagnostics.js` + المحرك
- **Trend weight بمتوسط متحرك 7 أيام** (لا قرارات على وزن صباحي عشوائي)، ومعدل أسبوعي بمقارنة نصفي النافذة (مع توثيق لإصلاح خطأ قسمة سابق كان يقلل المعدل للنصف).
- **TDEE تكيفي من موازنة الطاقة الفعلية** (الجسم يقول الحقيقة، المعادلة تقدّر فقط).
- **دورة 7 أسابيع مُدروسة:** تأقلم ألطف أول أسبوعين → تنشيف معتدل → **أسبوع استراحة دايت (MATADOR, Byrne 2018)** → إعادة؛ مع **Refeed يوم السبت** و**تدوير كارب** (تمرين 65% / راحة 48% / ريفيد 72% حول بروتين ثابت محمي).
- **تشخيص ثبات الوزن بتسعة أسباب متنافسة** (الالتزام، تسجيل ناقص، احتباس ماء، NEAT، تأقلم أيضي، النوم/الضغط، التمرين، قِدم الخطة، إرهاق الدايت) — مع **قفل أمان: ممنوع خفض السعرات لو السبب الأساسي غير غذائي** (لو واحد مش بيسجل أو نايم 5 ساعات، تقليل الأكل بيزود المشكلة). مدعوم بمراجع: Thomas 2014، Ostendorf 2021، Trexler 2014، StatPearls.
- **القرار يُطبَّق فعلًا:** قرارات المدرب تُحفظ في جدول `coach_targets` (ذاكرة دائمة) وتُحقن في حساب الخطة عبر `applyCoachAdjustment` بحدود أمان: ±20% من رقم المحرك، بروتين لا يُمَس، أرضية دهون 0.5جم/كجم، أرضية سعرات حسب السن.

### إجابات أسئلتكم الأربعة (موثقة بالكود)
| السؤال | الحكم |
|---|---|
| علم وقواعد للحالات المختلفة (ثبات/زيادة/ركود/تطور عضلة)؟ | **نعم** — سيناريوهات مغطاة بقواعد مبنية على أدلة منشورة، مع رفض آمن للمدخلات المستحيلة (422) بدل التخمين |
| بيقرأوا بيانات المستخدم ونتائجه وتطوره فعلًا؟ | **نعم** — أوزان (120)، تاريخ تغذية (120)، مقاسات (30)، جلسات ومجموعات التمرين، الخطوات، النوم، الضغط |
| ذاكرة/History كافية؟ | **نعم** — إعادة بناء حالة كل رفعة من السجل + coach_targets + prepared plans + computation store |
| قرارات وتعديل الخطة ولا ديكور؟ | **قرارات حقيقية تُطبَّق على الأرقام** (سعرات وماكروز وأحمال تتغير فعلًا) مع أقفال أمان — ليست نصائح معروضة فقط |

---

## 6) المتابعة الذكية — مُختبَرة فعليًا ON/OFF

كتبت اختبارًا وظيفيًا جديدًا (`test/smart-coach-toggle.test.js`) يشتغل ضد السيرفر الحقيقي عبر HTTP: تسجيل حساب → بروفايل كامل → 24 يوم أوزان راكدة (Plateau) → قراءة قرارات المحرك → قفل المفتاح → إعادة القراءة → فتحه مجددًا.

**النتيجة: 25/25 ✅** — والفرق بين ON وOFF حقيقي ومثبت:

| | ON (الافتراضي) | OFF |
|---|---|---|
| `bootstrap.smartCoach` | `true` | `false` (فورًا، بعد مسح الكاش) |
| تعديل السعرات التكيفي | قرار حر (زيادة/تقليل/تثبيت حسب التشخيص) | يتحول قسريًا إلى `hold` بدلتا صفر و`blockedBy=smart_coach_off` |
| الهدف التكيفي `adaptiveTargetCals` | رقم مُقترح من TDEE المقاس | `null` |
| اقتراح أحمال التمرين (exercise-history) | `coach` بتوصية الوزن/التكرارات/Deload | `coach: null` — تطوير يدوي |
| الاستمرارية | — | الحالة محفوظة في `user_prefs` بالسيرفر وتنجو من إعادة الفتح — **ليست Toggle في الواجهة فقط** |
| الرجوع ON | تستأنف كل القرارات من النقطة الحالية | — |

**الخلاصة القاطعة:** الزر مربوط بالمنطق الحقيقي في السيرفر — وOFF يوقف التطوير التلقائي للتمرين والتغذية معًا دون لمس الخطة الحالية.

---

## 7) الأداء وقابلية التوسع — هل تتحمل 50,000 مستخدم متزامن؟

### نقاط قوة موجودة بالفعل (تستحق الذكر)
- كاش bootstrap لكل مستخدم/يوم (20ث، 2000 مدخل) + in-flight dedupe في العميل (5 أماكن كانت بتنادي أثقل طلب معًا).
- كاش خطة التغذية (90ث) مع **قناة إبطال عبر Upstash Redis** بين النسخ + كاش قراءات داخل الطلب الواحد.
- ضغط gzip للردود، rate limiting، طابور كتابة أوفلاين في العميل، عزل المخازن لكل حساب، `/api/health` + `/api/admin/ops` للمراقبة، وworker pool للمحرك.

### الحدود الحالية بصراحة هندسية
1. **Turso بوضع remote:** كل استعلام = رحلة شبكة عبر TLS. طلب bootstrap ≈ 15–30 استعلامًا متسلسلًا ≈ 1–2.5ث على خادم دافئ. هذا هو **السبب الأول لإحساس البطء العام** في التطبيق.
2. **عملية Node واحدة** على Render المجاني (512MB، vCPU مشترك، سكون) + **worker pool بحجم 2** (افتراضي مناسب للمجاني فقط).
3. **توليد جدول التمرين يعمل على الخيط الرئيسي** (`computeStablePlan` متزامن) — يحجب الحلقة أثناء التوليد (تحرّك للخلفية في إصلاح المشكلة 3، لكنه يظل حدّ توسع).
4. وضع replica في Turso معطّل حاليًا بسبب WalConflict موثق (القاعدة كُتبت شهورًا بوضع remote).

### الحكم الصادق
البنية الحالية **سليمة ومحترفة لآلاف المستخدمين** النشطين، لكن **50,000 متزامن** تحتاج خارطة الطريق التالية (بالترتيب):
1. Render مدفوع + نسخ متعددة خلف موازن (الكود عديم الحالة تقريبًا وجاهز لذلك — الجلسات في القاعدة والكاش له إبطال مشترك).
2. قاعدة Turso جديدة بوضع **embedded replica** (قراءات محلية فورية) — تتطلب dump/restore مدروسًا؛ أو الترقية لاحقًا إلى PostgreSQL (حدود الـ adapter موجودة و`pg` ضمن الاعتماديات).
3. رفع `ENGINE_POOL_SIZE` ونقل توليد جدول التمرين إلى الـ worker pool أيضًا.
4. CDN للأصول الثابتة والفيديوهات.
5. كاش Redis مشترك أعمق للقراءات الساخنة (bootstrap/plans) بدل L1 المحلي فقط.
6. اختبارات حمل دورية (k6) على `/api/mobile/bootstrap` و`/api/mobile/nutrition-plan` قبل كل إطلاق كبير.

---

## 8) نتائج الاختبارات

- **54 حزمة اختبار موجودة:** 48 ناجحة / 5 راسبة قبل تعديلاتي + 1 تأثر بها.
- **أصلحت ما تأثر بتعديلاتي فورًا:** `performance-116` (14/14) و`app-version-gate` (12/12) — عقود مصدرية حُدّثت بنفس مقصدها بعد توسيع التواقيع بباراميتر مهلة اختياري.
- **اختباران جديدان مضافان:** `plans-prepare` (عقد المسار الجديد: حفظ سريع 144ms، تجهيز الخطتين، dedupe، idempotency) و`smart-coach-toggle` (25/25).
- **إصلاح قديم ربح جانبي:** `mobile-nutrition` كان راسبًا في الأصل — يمر الآن بعد إصلاح مفتاح اليوم.
- **راسبات موجودة أصلًا وغير مرتبطة بالمشاكل المطلوبة (لم تتغير — موثقة للفريق):**
  - `advanced-minimum-122`: توقعات محتوى تمارين (عضلات يوم Push).
  - `encoding-integrity`: فحص ترميز UTF-8 لملفات مصدر معينة.
  - `nutrition-rules`: 24/25 — قاعدة مكونات وجبة الغداء.
  - `scale-security`: 45/46 — «مسار التاريخ موصول ومحمي — history is not exported».
  - أوصي الفريق بفتح تذكرة لكل واحدة — كلها قابلة للإصلاح المستقل.

---

## 9) الملفات المعدّلة / المضافة

| الملف | التغيير |
|---|---|
| `api/mobile.js` | مسار تجهيز الخطط `preparePlansNow` + dedupe لكل مستخدم + `requestDay()` لمحاذاة يوم الكتابة مع القراءة (3 معالجات) |
| `server.js` | تسجيل مسار `POST /api/mobile/plans/prepare` |
| `mobile/lib/api.dart` | `warmup()` + مهلة اختيارية في `_send` + `me/appVersion/UpdateGate.check` بمهلة + `preparePlansNow()` |
| `mobile/lib/screens/splash_screen.dart` | إيقاظ فوري + مهلة إقلاع 12ث + 5 محاولات [3,4,6,8,10] + رسائل مرحلية |
| `mobile/lib/screens/workout_screen.dart` | وضع التحديث الصامت للمحفزات الخلفية (لا شريط/لا بانر فوق جدول معروض) |
| `mobile/lib/screens/profile_setup_screen.dart` | حفظ سريع في نهاية الشات بدون تجهيز متزامن |
| `mobile/lib/screens/analysis_screen.dart` | تجهيز الخطط من صفحة التحليل (المكان الصحيح للانتظار) |
| `test/smart-coach-toggle.test.js` | **جديد** — إثبات ON/OFF فعلي عبر HTTP |
| `test/plans-prepare.test.js` | **جديد** — عقد مسار التجهيز الجديد |
| `test/performance-116.test.js` | تحديث عقد مصدري بنفس المقصد |
| `test/app-version-gate.test.js` | تحديث عقد مصدري بنفس المقصد |

**ملاحظة تشغيلية:** تعديلات Flutter فُحصت يدويًا بعناية وتجاوزت حراس العقود المصدرية، لكن لا يوجد Flutter SDK في بيئة المراجعة هذه — شغّلوا `flutter analyze` وبنائًا تجريبيًا واحدًا قبل الرفع (إجراء معتاد).

## 10) أولويات ما بعد هذا التسليم
1. (أسبوع 0) فعّلوا مراقبة خارجية لـ `/api/health` أو انتقلوا لخطة Render مدفوعة — يلغي السبب الجذري للمشكلة 1 تمامًا.
2. (أسبوع 1) أغلقوا الراسبات الأربعة الموجودة أصلًا (قائمة مفصلة أعلاه).
3. (شهر 1) اختبار حمل k6 + نقل توليد جدول التمرين للـ worker pool.
4. (عند النمو) Turso replica على قاعدة جديدة أو PostgreSQL خلف الـ adapter الجاهز.
